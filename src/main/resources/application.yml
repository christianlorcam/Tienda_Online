spring:
  application:
    name: tiendaonline
  datasource:
    url: jdbc:postgresql://localhost:5432/tiendaonline
    username: christianlorca
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true